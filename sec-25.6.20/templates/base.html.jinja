<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Flask Feedback - {% block title %}{% endblock %}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
  <nav class="navbar bg-primary justify-content-start px-2 py-0">
    <a href="/" class="navbar-brand text-light"><span class="fs-3">F</span>lask <span class="fs-3">F</span>eedback</a>
    {% if session.get("username") %}
      <span class="nav-link px-3"><a href="/users/{{ session.get('username') }}" class="nav-link">Profile</a></span>
      <span class="nav-link px-3"><a href="/secret" class="nav-link">Secret</a></span>
      <form action="/logout" method="post">
        <button type="submit" class="nav-link px-3">Sign Out</button>
      </form>
    {% else %}
      <span class="nav-link px-3"><a href="/login" class="nav-link">Sign In</a></span>
      <span class="nav-link px-3"><a href="/register" class="nav-link">Register</a></span>
    {% endif %}
  </nav>
  {% include "partials/flashes.html.jinja" %}
  <main class="container-fluid">
    {% block content %}{% endblock %}
  </main>
</body>
</html>